# Cross-vendor fidelity expectations: dell_os10 -> fortigate_cli
#
# Source: dell_os10 codec.  Target: fortigate_cli codec.
#
# Every disposition below is resolved through the audit's own
# `actual_disposition()` over a measured `tools/run_full_mesh.py`
# pass, NOT read off the two capability matrices.  That ordering
# is forced by the per-pair unevidenced ratchet
# (`test_no_new_pair_declares_a_loss_it_never_observes`), which
# allows a NEW pair exactly zero declared-but-never-observed
# losses: a field this corpus preserves on every cell MUST be
# `good`, so hedging to `lossy` when unsure fails the build just
# as surely as missing a real loss does.  The matrices supply
# only the MECHANISM prose for a loss the corpus exhibits.
#
# Measured: 1 fixture cell, 0 render errors, 0 re-parse errors.
# Dispositions: {'unsupported': 7, 'good': 12, 'not_applicable': 8, 'lossy': 19}
#
# Schema spec: tests/fixtures/cross_vendor_expectations/README.md
# Vendor reference cache: docs/vendor-references/dell_os10_to_fortigate_cli/
#
# Authored 2026-09-21 with the dell_os10 registration (Phase 4).

meta:
  source_vendor: dell_os10
  target_vendor: fortigate_cli
  primary_use_case: >
    Migrating a Dell EMC PowerSwitch running SmartFabric OS10 onto
    FortiGate. OS10's supported surface is hostname, interfaces (three-
    segment `ethernet1/1/1` naming, CIDR addressing, `ip vrf
    forwarding`, L2 switchport and `channel-group`), SVI-derived VLANs,
    `port-channel` LAGs, VRFs, static routes including the separate
    `management route` RIB, local users, SNMP v2c/v3 and real VRRP. This
    file records which of those survive the render into FortiGate and
    which do not.
  certainty: medium
  references:
    - id: os10-fortigate-surface
      path: docs/vendor-references/dell_os10_to_fortigate_cli/canonical_surface.md
      title: "Dell OS10 -> FortiGate: measured canonical surface (1 fixture cell)"
      source_url: "https://github.com/netcanon/netcanon"
      retrieved: "2026-09-21"

# -------------------------------------------------------------------------

per_field_expectation:

  anycast_gateway_mac:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. NOT EXERCISED BY THIS
      CORPUS — all 1 cells are empty on both sides, so this
      disposition rests on the target codec's own capability
      declaration rather than on an observed round-trip. System-wide
      anycast-gateway MAC parses-and-ignores in v1. Schema exists on
      CanonicalIntent; wire-up scheduled for v0.2.0 Wave C. Measured
      over 1 cell: drifted on 0, preserved on 0, both sides empty on
      1.
    references: [os10-fortigate-surface]

  apply_groups:
    disposition: good
    note: >
      No cell populates this field on either side (1 of 1 trivially
      empty), and neither codec declares the surface unsupported, so
      nothing is claimed lost.
    references: [os10-fortigate-surface]

  dhcp_servers:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  dns_servers:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  domain:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  evpn_type5_routes:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  group_content:
    disposition: good
    note: >
      No cell populates this field on either side (1 of 1 trivially
      empty), and neither codec declares the surface unsupported, so
      nothing is claimed lost.
    references: [os10-fortigate-surface]

  hostname:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  interfaces[].description:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. FortiOS limits alias to 25 characters; longer descriptions
      from other vendors will be truncated. Measured over 1 cell:
      drifted on 1, preserved on 0, both sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].enabled:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].interface_type:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. FortiOS has no IANA ifType; inferred from 'type vlan' sub-
      setting or name shape. Measured over 1 cell: drifted on 1,
      preserved on 0, both sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].ipv4_addresses:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].ipv6_addresses:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].lag_member_of:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].mtu:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].name:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  interfaces[].vrrp_groups:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  lags:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  local_users:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  local_users[].hashed_password:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  local_users[].name:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  local_users[].role:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  ntp_servers:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  radius_servers:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  raw_sections:
    disposition: good
    note: >
      No cell populates this field on either side (1 of 1 trivially
      empty), and neither codec declares the surface unsupported, so
      nothing is claimed lost.
    references: [os10-fortigate-surface]

  routing_instances:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0. Sample drift: all 2 routing_instances dropped.
    references: [os10-fortigate-surface]

  routing_instances[].description:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. The OS10 `ip vrf <name>` stanza carries no description
      line; the VRF renders, its description does not. Measured over 1
      cell: drifted on 1, preserved on 0, both sides empty on 0.
    references: [os10-fortigate-surface]

  routing_instances[].name:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  snmp.community:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  snmp.contact:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  snmp.location:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  snmp.trap_hosts:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  snmp.v3_users:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  static_routes:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0. Sample drift: {"static_routes[0]
      {'destination': '0.0.0.0/0'}": {'vrf': {'source': 'management',
      'target': ''}}, "static_routes[2] {'destination':
      '10.50.0.0/16'}": {'vrf': {'source': 'TENANT-A', 'target':
      ''}}}.
    references: [os10-fortigate-surface]

  syslog_servers:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. NOT EXERCISED BY THIS
      CORPUS — all 1 cells are empty on both sides, so this
      disposition rests on the target codec's own capability
      declaration rather than on an observed round-trip. Render emits
      no logging/syslog config; intent.syslog_servers are dropped on
      migration. Measured over 1 cell: drifted on 0, preserved on 0,
      both sides empty on 1.
    references: [os10-fortigate-surface]

  timezone:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. NOT EXERCISED BY THIS
      CORPUS — all 1 cells are empty on both sides, so this
      disposition rests on the target codec's own capability
      declaration rather than on an observed round-trip. Render emits
      no clock/timezone stanza; intent.timezone is dropped on
      migration. Measured over 1 cell: drifted on 0, preserved on 0,
      both sides empty on 1.
    references: [os10-fortigate-surface]

  vlans[].description:
    disposition: good
    note: >
      No cell populates this field on either side (1 of 1 trivially
      empty), and neither codec declares the surface unsupported, so
      nothing is claimed lost.
    references: [os10-fortigate-surface]

  vlans[].id:
    disposition: good
    note: >
      Preserved on 1 of 1 cells; no cell drifts. Both codecs model
      this surface.
    references: [os10-fortigate-surface]

  vlans[].ipv4_addresses:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  vlans[].name:
    disposition: lossy
    reason: >
      The anchor renders but the value does not survive the round
      trip. Measured over 1 cell: drifted on 1, preserved on 0, both
      sides empty on 0.
    references: [os10-fortigate-surface]

  vlans[].tagged_ports:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. FortiOS binds a VLAN to
      a single parent child-interface; multi-port tagged membership
      (switchport's VLAN-centric twin) is dropped on render. Measured
      over 1 cell: drifted on 1, preserved on 0, both sides empty on
      0.
    references: [os10-fortigate-surface]

  vlans[].untagged_ports:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. FortiOS binds a VLAN to
      a single parent child-interface; multi-port untagged membership
      (switchport's VLAN-centric twin) is dropped on render. Measured
      over 1 cell: drifted on 1, preserved on 0, both sides empty on
      0.
    references: [os10-fortigate-surface]

  vxlan_vnis:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. NOT EXERCISED BY THIS
      CORPUS — all 1 cells are empty on both sides, so this
      disposition rests on the target codec's own capability
      declaration rather than on an observed round-trip. VXLAN not
      modelled — FortiGate is a firewall codec. Measured over 1 cell:
      drifted on 0, preserved on 0, both sides empty on 1.
    references: [os10-fortigate-surface]

  vxlan_vnis[].mcast_group:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  vxlan_vnis[].vlan_id:
    disposition: not_applicable
    note: >
      Measured empty on both sides of all 1 cells: Dell OS10 declares
      this surface unsupported, so it never enters the canonical tree
      from this source. Re-author it on the target rather than assume
      there was nothing there.
    references: [os10-fortigate-surface]

  vxlan_vnis[].vni:
    disposition: unsupported
    reason: >
      FortiGate renders no instance of this surface, so the record
      cannot exist on the target side at all. NOT EXERCISED BY THIS
      CORPUS — all 1 cells are empty on both sides, so this
      disposition rests on the target codec's own capability
      declaration rather than on an observed round-trip. VXLAN not
      modelled — FortiGate is a firewall codec. Measured over 1 cell:
      drifted on 0, preserved on 0, both sides empty on 1.
    references: [os10-fortigate-surface]
