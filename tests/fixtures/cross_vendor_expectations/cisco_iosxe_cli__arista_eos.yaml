# Cross-vendor fidelity expectations: cisco_iosxe_cli -> arista_eos
#
# This file enumerates the per-field translation disposition for every
# top-level field on CanonicalIntent (see netcanon/migration/canonical/
# intent.py).  Each disposition is grounded in vendor documentation
# excerpts under docs/vendor-references/cisco_iosxe_cli_to_arista_eos/.
#
# Schema authored by Agent A in tests/fixtures/cross_vendor_expectations/
# README.md (sibling file).  Disposition vocabulary:
#   good            - field round-trips without semantic loss
#   lossy           - field round-trips with documented degradation
#   unsupported     - field is parse-and-ignore on one or both codecs
#   not_applicable  - field has no concept on the target vendor
#
# Phase 3b deliverable.  Cisco IOS-XE -> Arista EOS is the second
# worked-example pair after the arista_eos -> juniper_junos pair (Agent
# A).  This pair is high-feature-overlap (DC-to-DC migration; Arista
# EOS deliberately mirrors Cisco IOS for low onboarding friction).

meta:
  source_vendor: cisco_iosxe_cli
  target_vendor: arista_eos
  primary_use_case: >
    Cisco IOS-XE -> Arista EOS DC migration; high feature overlap
    (Arista EOS deliberately mirrors Cisco IOS CLI grammar for
    low operator onboarding cost).  The keyword-stable surface
    (vlans / switchport / static routes / SNMP v1/v2c / hostname /
    NTP / DNS / logging) round-trips cleanly; semantic-divergent
    surfaces (interface naming, VRF, local user hashes, routing
    protocols) are flagged lossy or unsupported.
  certainty: medium
  references:
    - id: vlans
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/vlans.md
      title: "VLAN configuration: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-virtual-lans-vlans"
      retrieved: "2026-04-30"
    - id: switchport
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/switchport.md
      title: "Switchport mode: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-virtual-lans-vlans"
      retrieved: "2026-04-30"
    - id: interface_naming
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/interface_naming.md
      title: "Interface naming: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.cisco.com/c/en/us/td/docs/ios-xml/ios/interface/command/ir-cr-book/ir-s7.html"
      retrieved: "2026-04-30"
    - id: ip_addressing
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/ip_addressing.md
      title: "IPv4 / IPv6 addressing: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-ipv4"
      retrieved: "2026-04-30"
    - id: static_routes
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/static_routes.md
      title: "Static routes: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-ipv4"
      retrieved: "2026-04-30"
    - id: vrf
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/vrf.md
      title: "VRF / routing-instance: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-static-inter-vrf-route"
      retrieved: "2026-04-30"
    - id: snmp
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/snmp.md
      title: "SNMP: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-snmp"
      retrieved: "2026-04-30"
    - id: local_users
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/local_users.md
      title: "Local users + hash formats: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-user-security"
      retrieved: "2026-04-30"
    - id: aaa
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/aaa.md
      title: "AAA / TACACS+ / RADIUS: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-user-security"
      retrieved: "2026-04-30"
    - id: spanning_tree
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/spanning_tree.md
      title: "Spanning-tree mode: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-spanning-tree-protocol"
      retrieved: "2026-04-30"
    - id: routing_protocols
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/routing_protocols.md
      title: "Routing protocols (BGP / OSPF / EIGRP): Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-border-gateway-protocol-bgp"
      retrieved: "2026-04-30"
    - id: system_services
      path: docs/vendor-references/cisco_iosxe_cli_to_arista_eos/system_services.md
      title: "System services: Cisco IOS-XE versus Arista EOS"
      source_url: "https://www.arista.com/en/um-eos/eos-switch-administration-commands"
      retrieved: "2026-04-30"

# ---------------------------------------------------------------------------
# Per-field expectations.  One entry per top-level CanonicalIntent field;
# walks intent.py top-down.
# ---------------------------------------------------------------------------

per_field_expectation:

  # ── Tier 1 — auto-translatable scalars / lists ──

  hostname:
    disposition: good
    note: >
      Both vendors use a bare ``hostname <name>`` global directive.
      Identical grammar; round-trip is lossless.

  domain:
    disposition: good
    note: >
      Cisco uses ``ip domain name <fqdn>`` (space) and Arista uses
      ``ip domain-name <fqdn>`` (hyphen).  Both codecs handle the
      keyword variation; canonical stores the FQDN string verbatim.
    references: [system_services]

  dns_servers:
    disposition: good
    note: >
      Both vendors use ``ip name-server <addr>`` directives, one
      per server.  Round-trip is lossless on the address list.
    references: [system_services]

  ntp_servers:
    disposition: lossy
    reason: >
      Canonical model stores ``ntp_servers: list[str]`` (bare
      addresses only).  Per-server options (``prefer``, ``iburst``,
      ``key``, ``source``) are not modelled and drop on round-trip.
      Address list itself is preserved.
    references: [system_services]

  timezone:
    disposition: lossy
    reason: >
      Cisco emits ``clock timezone PST -8 0`` (offset+DST tokens)
      while Arista emits ``clock timezone US/Pacific`` (zoneinfo
      name).  Canonical stores an opaque string; cross-vendor
      migration may produce a token the target accepts syntactically
      but with different DST behaviour.  Operator-curated mapping
      table would be needed for full fidelity.
    references: [system_services]

  syslog_servers:
    disposition: good
    note: >
      Both vendors use ``logging host <addr>`` (or legacy ``logging
      <addr>``) and round-trip the server list cleanly.  Severity /
      facility tokens are not modelled (canonical surface is host
      list only).
    references: [system_services]

  interfaces:
    disposition: lossy
    reason: >
      The interface list itself round-trips; per-interface fields
      (name, description, enabled, ipv4_addresses, ipv6_addresses,
      switchport_mode, access_vlan, trunk_allowed_vlans,
      trunk_native_vlan, voice_vlan, lag_member_of, dhcp_client)
      are good.  The lossy bit is ``interface_type`` / speed-hint:
      Cisco names encode speed (GigabitEthernet vs TenGigabitEthernet)
      while Arista's ``Ethernet<N>`` does not, so cross-vendor
      round-trip degrades the speed inference.  Documented in both
      codecs' capability matrices.  ``vrf`` field on per-interface
      records also degrades — see ``routing_instances`` below.
    references: [interface_naming, switchport, ip_addressing]

  vlans:
    disposition: good
    note: >
      VLAN id / name / description / tagged_ports / untagged_ports /
      ipv4_addresses (SVI) round-trip cleanly.  Both vendors use
      ``vlan <N> / name <X>`` and the same trunk syntax.  Port lists
      track the renamed interface names from the rename mesh.
    references: [vlans, switchport]

  static_routes:
    disposition: lossy
    reason: >
      Default-VRF static routes round-trip cleanly (Cisco's dotted-
      mask form converts to / from Arista's CIDR form via the
      codecs).  Per-VRF static routes (``ip route vrf X ...``)
      now round-trip too: both codecs harvest and render the
      per-VRF form via ``CanonicalStaticRoute.vrf`` (Cisco
      ``ip route vrf X`` <-> Arista ``ip route vrf X``).  The
      ``lossy`` disposition is a conservative holdover from
      before the v0.2.0 vrf wire-up; a candidate for promotion
      to ``good``.
    references: [static_routes, vrf]

  # ── Tier 2 — auto-translate with review ──

  dhcp_servers:
    disposition: lossy
    reason: >
      Cisco's ``ip dhcp pool <name>`` syntax with ``network``,
      ``default-router``, ``dns-server``, ``lease`` lines maps to
      Arista's IP DHCP Server / IP DHCP Relay configuration.
      The Cisco codec parses DHCP pools (per its capability matrix);
      the Arista codec does not advertise ``/dhcp/pool`` paths in
      its supported set.  Cross-vendor round-trip therefore drops
      DHCP server config on Cisco -> Arista.  Deferred to subsequent
      audit pass.

  snmp:
    disposition: lossy
    reason: >
      v1 / v2c surface (community / location / contact / trap_hosts)
      round-trips cleanly — both vendors share the
      ``snmp-server community / location / contact / host`` grammar.
      v3 USM (v3_users) round-trips the username + group + protocol
      enum (auth_protocol / priv_protocol) but the opaque
      auth_passphrase / priv_passphrase blobs are NOT cross-
      compatible: USM keys are salted with vendor-specific engineID
      constants, so Cisco-derived passphrases will fail on Arista
      after migration.  Operator must re-key v3 users on the target
      device.  Documented in CanonicalSNMPv3User docstring.
    references: [snmp]

  lags:
    disposition: lossy
    reason: >
      Both vendors use ``channel-group <N> mode active`` (LACP) on
      the member interface, but the resulting LAG name differs:
      Cisco emits ``Port-channel<N>`` (lower-case 'c'), Arista emits
      ``Port-Channel<N>`` (capital 'C').  The Arista codec docstring
      explicitly notes this divergence.  Round-trip preserves
      member lists and LACP mode but the LAG name differs by
      capitalisation, which the rename mesh canonicalises if the
      target side uses port-name overrides.

  local_users:
    disposition: lossy
    reason: >
      Cisco's ``privilege <N>`` (numeric, 1-15) versus Arista's
      ``role <name>`` (named RBAC) requires operator-curated mapping
      on round-trip.  More fundamentally, the password hash formats
      are NOT cross-compatible:  Cisco type-9 scrypt (``$9$...``) is
      Cisco-only; Arista sha512-crypt (``$6$...``) is Arista-only;
      only the legacy MD5 type-5 (``$1$...``) form lands on both.
      Cross-vendor migration of user accounts therefore requires
      re-setting passwords on the target device.  Both codecs pass
      hashes through verbatim; the loss surfaces in the validation
      report.
    references: [local_users]

  radius_servers:
    disposition: unsupported
    reason: >
      Phase 4 evidence (kitchen_sink fixture: source has 2 RADIUS
      servers; target has 0) shows the ``arista_eos`` codec has no
      ``radius-server host`` parse or render path today — the cross-
      pair drops the canonical RADIUS server list entirely.  Demote
      from ``good`` to ``unsupported`` until the Arista codec wires
      ``radius-server host`` parse + render.  Was previously documented
      as round-tripping cleanly via the
      ``host / key / auth_port / acct_port`` tuple, but the wire-up
      assumption was wrong.
    references: [aaa]

  # ── Tier 2 (ship-before-wire) — EVPN-VXLAN fabric schema ──

  vxlan_vnis:
    disposition: lossy
    reason: >
      Promotion #16 wired IOS-XE VXLAN-EVPN: the codec now intercepts
      ``interface nve1`` and correlates the ``vlan configuration N /
      member [evpn-instance M] vni V`` bindings, so the L2 VLAN↔VNI
      mapping + VTEP source-interface survive to Arista ``vlan N vxlan
      vni X`` inside ``interface Vxlan1``.  The one sub-detail that drops
      on this direction is the per-VNI multicast group (dropped by the
      Arista render), so the field is lossy overall (see
      vxlan_vnis[].mcast_group).

  vxlan_vnis[].vlan_id:
    disposition: good

  vxlan_vnis[].vni:
    disposition: good

  vxlan_vnis[].source_interface:
    disposition: good

  vxlan_vnis[].mcast_group:
    disposition: lossy
    reason: >
      IOS-XE ``member vni N mcast-group <addr>`` maps to Arista ``vlan N
      multicast-group <addr>``, but the Arista render currently drops the
      per-VNI multicast group, so it does not survive the cross-pair.

  vxlan_vnis[].flood_list:
    disposition: lossy
    reason: >
      Head-end static ingress-replication peer IPs have no per-VNI nve1
      grammar on the IOS-XE side (declared lossy on the codec), so an
      explicit flood_list drops on the IOS-XE source render.

  evpn_type5_routes:
    disposition: unsupported
    reason: >
      Both codecs list ``/evpn-type5-routes/route`` under lossy with
      rationale that EVPN Type-5 is a VRF-property model
      (CanonicalRoutingInstance.l3_vni) rather than per-prefix
      records; no codec populates the per-prefix list today.
      Effectively unsupported on the cross-pair until route-map /
      policy-statement parsing lands.

  routing_instances:
    disposition: unsupported
    reason: >
      Cisco IOS-XE CLI codec capability matrix lists
      ``/routing-instances/instance`` as LOSSY (not unsupported):
      ``vrf definition <name>`` blocks (``rd`` + ``address-family
      ipv4`` + ``route-target import/export``) parse into
      ``CanonicalIntent.routing_instances`` via
      ``parse._parse_routing_instances``, and Arista EOS advertises
      the path as supported — its render emits each L3 VRF as ``vrf
      instance <name>`` (GAP-6 landed; ``arista_eos/render.py``) with
      RD + route-targets under ``router bgp / vrf``.  So VRF name, RD
      and route-targets DO cross iosxe_cli -> arista.  The round-trip
      is not lossless: the IOS-XE parse ignores per-VRF
      ``address-family ipv6`` / EVPN ``l2vpn evpn`` sub-stanzas, and
      the Arista render has no ``vrf instance / description`` emit path
      so a parsed VRF description is dropped.
    references: [vrf]

  # ── Tier 3 — informational only (never auto-rendered) ──

  raw_sections:
    disposition: not_applicable
    note: >
      ``raw_sections`` is a Tier-3 dict of section-name -> raw text
      that the pipeline carries through for display but never auto-
      renders.  It is by-design vendor-specific and does not map
      across vendors.  The validation report surfaces sections for
      operator review but cross-vendor translation is out of scope
      for this field.

  # ── Metadata ──

  source_vendor:
    disposition: not_applicable
    note: >
      Set by the source codec on parse; the target codec does not
      consume it (cross-vendor round-trip explicitly changes
      vendor).  Not a translation field.

  source_format:
    disposition: not_applicable
    note: >
      Same as source_vendor — informational metadata only.

  source_version:
    disposition: not_applicable
    note: >
      OS version hint from the parser; not a translation field.

  # ── Vendor-provenance hints ──

  apply_groups:
    disposition: not_applicable
    note: >
      Junos-specific (``apply-groups`` statement preservation per
      GAP 9b).  Cisco IOS-XE and Arista EOS codecs do not emit
      apply_groups; field is always empty on this cross-pair.

  group_content:
    disposition: not_applicable
    note: >
      Junos-specific (group-scoped set-line tails).  Same as
      apply_groups — empty on this cross-pair.

# ---------------------------------------------------------------------------
# Notes on confidence
# ---------------------------------------------------------------------------
#
# certainty=medium because:
#
#   * The keyword-stable surface (vlans / switchport / static routes /
#     hostname / DNS / NTP / SNMP v1+v2c) is high-confidence: the EOS
#     manual examples are byte-identical to Cisco IOS for these
#     commands, and both codecs have established round-trip tests on
#     real captures.
#
#   * The interface-naming / VRF / hash-format / routing-protocol
#     surfaces are medium-confidence: the vendor docs are clear on
#     syntax but the codec wire-up is partial (e.g. Cisco IOS-XE
#     parses VRF declarations but drops per-VRF address-family-ipv6 /
#     EVPN sub-stanzas; Arista EOS renders `vrf instance` but drops
#     the VRF description).
#
#   * Spanning-tree, BGP, OSPF, EIGRP are explicitly out of canonical
#     scope on both vendors and confidently marked unsupported.
#
#   * DHCP server config is a known gap (Cisco parses, Arista codec
#     does not advertise the path) and confidently marked lossy
#     pending wire-up.
#
# Deferred items (not researched in depth this pass):
#
#   * Per-VRF static routes (``ip route vrf X ...``) — Cisco parses
#     and emits them, Arista same, now bound to
#     ``CanonicalStaticRoute.vrf`` (v0.2.0); round-trips cleanly.
#     The static_routes cell above is conservatively still lossy.
#
#   * NTP per-server options (prefer / iburst / key / source) —
#     canonical model is host-list-only.  Lossy by design;
#     subsequent audit pass would extend the model if demand
#     surfaces.
#
#   * TACACS+ servers — no canonical model in v1.  Lands when
#     CanonicalTACACSServer is added.
